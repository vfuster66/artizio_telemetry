name: artizio_telemetry
description: Couche observabilité Artizio — crash, erreurs, analytics, diagnostic (Sentry derrière).
publish_to: "none"
version: 0.2.0

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ">=8.3.1 <11.0.0"
  sentry_flutter: ^9.26.0
  shared_preferences: ^2.3.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
